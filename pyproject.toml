[project]
name = "sports-card-market-os"
version = "0.7.4"
requires-python = ">=3.11"
dependencies = [
  "python-dateutil>=2.8",
  "requests>=2.31",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["bulk_ingest", "data_provider", "delta_engine", "detectors", "ebay_ingest", "engine", "entity_matcher", "evidence_store", "evidence_to_warehouse", "feature_engineering", "grader", "import_bundle", "market_contract", "market_engine", "memory", "models", "scan", "scoring", "warehouse"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
